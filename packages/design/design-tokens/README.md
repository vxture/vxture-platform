# @vxture/design-tokens

Vxture Design System 的 token 层：T1 foundation（Tailwind v4 镜像）与 T2 semantic（唯一公开 token 契约），policy 驱动生成。

一般不直接消费本包——应用统一从伞包 `@vxture/design-system` 一个入口引。

使用规范随伞包发布：见 `@vxture/design-system` 包内 `docs/`，T2 全族档位清单在 `docs/04-tokens-contract.md`。

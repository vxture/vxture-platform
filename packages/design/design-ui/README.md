# @vxture/design-ui

Vxture Design System 的纯 UI 组件层：基础组件、平台图案、模板骨架、图标隔离层。零业务、零平台依赖。

一般不直接消费本包——应用统一从伞包 `@vxture/design-system` 一个入口引。

使用规范随伞包发布：见 `@vxture/design-system` 包内 `docs/`（使用契约 / 视觉规格 / 模式选用 / token 契约 / 内容 / 无障碍）。

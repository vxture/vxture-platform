{
  "name": "@vxture/core-database",
  "version": "1.0.0",
  "private": true,
  "description": "Vxture platform — Prisma Client generation from the platform schema. Authoritative migrations + seed live in deploy/ (single source).",
  "scripts": {
    "generate": "prisma generate --schema=prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}

# @vxture/service-model-platform — Retired

**Retired 2026-07-28.** This package (`services/model/platform/`) was extracted to the standalone `vxture-atlas` repo during the 2026-07-24 repo split, then deleted from this repo once production traffic (`MODEL_PLATFORM_URL`) was confirmed cut over to the external atlas host.

Current architecture pointer: [`docs/30-design/platform/40-model-platform.md`](../../../30-design/platform/40-model-platform.md).
Package-level design/contract for the extracted service is now authoritative in `vxture-atlas`, not here.

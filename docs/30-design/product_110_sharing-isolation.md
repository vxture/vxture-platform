# Vxture 共享与隔离模型:数据·知识·能力(Sharing & Isolation Model)(product_110)

> 版本:**v1.1**
> 状态:**已定稿**(v1.0 = 2026-07-06 owner 拍板,待拍板项全部销号;v1.1 = 2026-08-10 按 runos [`ADR-003`](https://github.com/vxture/vxture-runos/blob/main/docs/30-design/decisions/ADR-003-commercial-capability-plane.md)(owner 决定 2026-08-07)对 §6/§7 作**收窄改写**,办理 = 本仓 issue [#216](https://github.com/vxture/vxture-platform/issues/216),见 §12#12)
> 文档族:产品架构族 `product_{NNN}`,本文 = **110**(架构位);族路由见 [`product_100_matrix.md`](./product_100_matrix.md) §0 头部
> 上游文档:[`product_100_matrix.md`](./product_100_matrix.md) v1.2(产品矩阵与分层,本仓重构)、[`ADR-11`](./decisions/ADR-011-subscription-entitlement-design.md)(订阅/权益)、[`ADR-12`](./decisions/ADR-012-sharing-grant-design.md)(共享授权决策记录)
> 伴生文档:[`product_200_integration.md`](./product_200_integration.md)(vxture×产品三通道对接标准)
> 适用范围:L2 域平台(Arda / Karda / Terra)统一原型、Runos 产品边界、L0 工具协议规范、Karda 产品定义前置输入、L3 agent 数据下沉规范
> 留档:v0.2 原稿见 [`inputs/sharing-isolation-model-v0.2.md`](./inputs/20-sharing-isolation-model-v0.2.md);评审与对账见 [`sharing-isolation-review.md`](./design_sharing_100_isolation-review.md)
>
> **v1.1 变更摘要**(相对 v1.0,2026-08-10;依据 = runos `ADR-003` + issue #216):
>
> 1. **Runos 两域模型**(§6.1):**联邦域 = v1.0 现模型,原样保留**——唯一直连、无中心瓶颈、无 ESB、提供方侧求值四条一字不改;**新增商业能力域**——以订阅独立交付的业务场景 agent 经 Runos 网关消费非模型能力。本次是**收窄改写(scoping),不是反转**;
> 2. **Runos 定位改写为「商业能力面」**(§6.1):四原语(连接器/技能/执行器/资产)、两段裁决(opera 技术供给 → admin 商业封装);**控制面不在调用链路,商业域数据面(网关 + 执行器沙箱)在调用链路上**;
> 3. §6.3 增补商业域数据面职责;§6.4 负面清单逐条按域收窄(保留"不是 L2 的 API 网关/不是模型平台/不是工作流引擎"三条两域皆真);§6.6/§6.7 结论按域分列(联邦域:执行=无、计量=零;商业域:网关+沙箱在链路上、有配额与计量);§7.2 联邦域唯一直连原样成立并增补商业域为第二个域;§7.3 总图按域分列;
> 4. **未决项登记**(§6.8 新增):商业域计量归属与去重、org 内 SharingGrant 在商业域的求值点——**商业域首个可售能力包上线前须收口**;
> 5. §11/§13 相应条款按域收窄;§12 增 #12 记本次改写。
>
> **v1.0 变更摘要**(相对 v0.2):
>
> 1. **产品名称定为终版**(owner 拍板,稳定使用):L1 = Atlas / Ontos / Runos,L2 = Arda / Karda / Terra,L3 = Raven / Anlan / Forge / Xuanzhen;原引用的外部上游 product-matrix v1.1 / ADR-entitlement-and-workspace v2 由本仓 [`product_100_matrix.md`](./product_100_matrix.md) + ADR-11/12 重构取代;
> 2. **ruyin / umbra 重定义**:现平台在 ruyin.ai 对接的产品实为 **umbra(边界 VPN)**,域名不变,保持现状订阅模式;**Ruyin 重新定义为 client 端产品(desktop)**,§11 边界条款随之改写;
> 3. **P 级资产范围收窄 + 供给形态二分 + 来源审计强化**(§4.2):P 级 = 行业公开数据 + 三方公开服务数据,敏感数据不进平台;分「自有资产」与「代理持牌服务」两形态;全生命周期来源/许可审计;
> 4. **Runos「无运行时」精确化**(§6.7 控制面/运行时判定表)、eval 执行位置补全(§6.3);
> 5. **SharingGrant 细化**(§8):命中谓词写死、scope 按资产类型参数化、`expires_at`、多 grant 合成、级联撤销机制、**策略 SoT 落位平台控制面 `sharing` 域**、管理权模型转已固化;
> 6. §4.3 托管水位线表补「运行时托管」维度;§10 风险应对随上述细化更新。

---

## 1. 背景与目标

Vxture 为多租户 SaaS 平台,租户模型为 **org(tenant)→ 多个 workspace**,entitlement 粒度为 workspace × product(state × tier 双轴)。

L3 行业应用(Raven / Anlan / Forge / Xuanzhen)业务跨度大,各自建有专属数据库(**agent-db,一个产品一套,按 WS 隔离**);同时平台需要在 org 内实现数据、知识、能力的按需共享与复用。

本文档定义贯穿平台的统一共享与隔离模型,回答四个问题:

1. 什么是不可配置的硬边界,什么是可配置的软边界;
2. 三个 L2 域平台以什么统一模式托管与共享资产;
3. 能力如何供给 agent,Runos 在其中的角色是什么;
4. 共享如何表达、由谁授权、在哪里执行。

---

## 2. 核心设计原则

> **能力同构、数据异构、共享是策略而非结构。**

| 原则           | 含义                                                                                                                                                           |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **能力同构**   | 所有 L3 agent 消费同一套 L1/L2 能力面,无任何 agent 拥有私有能力;agent 间差异仅来自数据与场景编排。能力"存在性"同构,"可用性"由 entitlement 控制(feature-gating) |
| **数据异构**   | 数据按需下沉至 agent-db 或托管于 L2 域平台,归属始终清晰;托管程度由各对象域的数据结构收敛度决定(见 §4 托管水位线)                                               |
| **共享是策略** | 共享通过授权对象(SharingGrant / entitlement)表达,不产生数据副本;数据留在原地,可见性由策略动态计算,撤销即时生效、无残留                                         |
| **默认拒绝**   | org 内跨 WS / 跨 product 的访问默认关闭(default-deny),按需显式开放                                                                                             |
| **供给直连**   | L2 能力对 agent 的供给通道唯一为直连(L0 统一协议 + 各产品入口授权);不存在中心代理层                                                                            |

---

## 3. 两平面模型:硬隔离 + 软共享

### 3.1 技术平面(硬边界,不可配置)

- **org(tenant)是绝对隔离边界。** 所有存储(agent-db、Karda 索引、Arda 目录、Terra 租户空间数据、图谱实例)的数据键均携带 `org_id`;跨 org 的租户数据流动在架构上**不存在通路**——不是"权限不允许",而是"无此路径"。
- 唯一合法的跨 org 数据形态是 **P 级平台共有资产**(§4.2):平台供给、只读、经 entitlement 消费,不含任何租户数据。
- 该边界是 SaaS 安全底线,任何业务配置不得突破。已吸入平台数据架构铁律表(`data_platform_100` §2.2.3)。

### 3.2 业务平面(软边界,策略驱动)

- org 内部以 **WS 为共享单元**,通过权限配置实现按需开放。
- 隔离不靠数据搬运或复制实现,靠策略层的授权对象实现。
- 推论:**共享不产生数据副本**——WS-A 向 WS-B 开放某资产,仅在策略层登记一条 grant,访问时按调用方身份动态求交;撤销即时生效,审计只需策略变更记录。

---

## 4. L2 域平台统一原型(Archetype)

**L2 只有一种模式,三个产品(Karda / Terra / Arda)是同一模式在不同参数下的实例。** 这是本模型的核心公理。

### 4.1 原型结构

```
L2 域平台 = 能力层 + 分级资产层 + 授权层

能力层:域内的加工/计算/检索/治理能力
        以一方工具形式按 L0 统一协议直连暴露,entitlement 控制可用性
资产层:P 级 — 平台共有资产(跨 tenant、只读、产品级供给、entitlement 消费)
        T 级 — 租户资产(org/WS 隔离、SharingGrant 授权共享)
        A 级 — agent 专属数据(托管在域平台内 或 驻留 agent-db)
授权层:跨 tenant 走 entitlement;org 内走 SharingGrant;两套机制不混用
```

### 4.2 P 级平台共有资产(平台级通用机制)

P 级资产是 L2 通用概念,在 L0/本模型统一定义一次,三个 L2(及 Runos 的平台技能)复用。

**范围界定(v1.0 收窄,owner 拍板)**:P 级资产 = **行业公开数据 + 三方公开服务数据**(如高德地图、Google 影像、公开法规/标准库、行政区划等)。**敏感数据不进平台**——含个人信息、涉密、受管制(如测绘管制)的数据不得作为 P 级供给;需要此类数据的场景走租户自有(T/A 级)通道并由租户承担合规主体责任。

**供给形态二分**:

| 形态                               | 定义                                                            | 数据落点                                          | 典型                                                |
| ---------------------------------- | --------------------------------------------------------------- | ------------------------------------------------- | --------------------------------------------------- |
| **自有资产(asset)**                | 平台自持、版本化托管的行业公开数据                              | 平台侧(所属 L2 的产品数据面),版本化管理           | 法规/标准/通用 SOP 库、行政区划、行业代码、通用白膜 |
| **代理持牌服务(brokered service)** | 三方公开服务的代理接入,平台售卖的是**接入与额度**而非数据再分发 | 数据不落平台(或仅协议允许的缓存);许可主体在服务商 | 高德地图瓦片、Google/商业遥感影像服务、天地图       |

> 地图/遥感类受《测绘法》等监管,平台自持再分发不可行,一律按 brokered service 建模;许可与合规元数据指向服务商条款。

**通用机制**:

- **供给方**:Vxture 平台(产品级支持),跨 tenant 只读,版本化管理;
- **消费方式**:经 entitlement 消费(可作为独立 SKU 或 tier 权益售卖),**不走 grant**(grant 是 org 内机制);
- **元数据要求**:含**来源与许可合规元数据**字段——数据来源(provenance)、许可条款与证据、再分发范围、地域合规约束、版本;
- **来源审计(v1.0 强化,owner 拍板)**:P 级资产全生命周期审计——①**准入登记**(来源、许可证据、再分发范围、责任 operator,走上架准入流程,对齐 `product.launch_checklist` 模式);②**版本变更审计**;③**分发消费审计**(哪个 org/WS 经何 entitlement 消费、用量);④**许可到期/变更复审**。审计记录入平台中央审计(`support.audit_log`);
- **商业意义**:平台自有的知识/数据资产成为独立收入线,是纯能力平台不具备的。

### 4.3 托管水位线:三个 L2 的主变量

三个产品之间的主变量是 **A 级数据的托管水位线**——即 agent 专属数据有多大比例可被域平台托管。水位线由客观属性决定:**该对象域的数据结构收敛度**。另有一个正交的次变量:**运行时托管**(域平台是否托管持续运行的实例态)。

| 维度                    | Karda                                  | Terra                                  | Arda                                        |
| ----------------------- | -------------------------------------- | -------------------------------------- | ------------------------------------------- |
| 对象域                  | 非结构化知识                           | 时空/物理世界                          | 结构化业务数据                              |
| 数据结构收敛度          | 高(加工后表示统一:分块/向量/图谱/索引) | 分裂(底座标准统一、业务数据发散)       | 低(schema 随业务各异)                       |
| **托管水位线**          | **全量托管**                           | **托管标准化底座**                     | **托管通用件,其余汇聚**                     |
| **运行时托管**(次变量)  | 索引即运行时(检索不能脱离索引)         | 孪生实例运行态由 Terra 托管            | 无(目录与服务化,无常驻实例)                 |
| P 级资产示例            | 平台知识库(法规、标准、通用 SOP)=asset | 地图、遥感影像=brokered;通用白膜=asset | 通用参考/主数据(行政区划、行业代码等)=asset |
| A 级数据驻留            | Karda 内,以授权逻辑区分                | agent-db(保留比例按业务复杂度自定)     | agent-db 为主                               |
| agent 是否需自建域内 db | **基本不需要**                         | 需要,规模按业务定                      | 需要(即 agent-db 本身)                      |
| 隔离手段                | 授权逻辑 + 索引命名空间(§10#4 分档)    | 平台底座跨 tenant / 租户数据隔离       | 库级物理隔离 + 目录授权                     |

---

## 5. 三个 L2 的参数化定位

### 5.1 Karda:知识能力域 + 全量知识资产托管

**定位**:企业知识能力域——知识加工、检索与治理能力,托管平台/组织/agent 三级知识库,整体被 agent 集成应用。

```
Karda(L2 知识能力域)
├── 能力层(Knowledge Engine)
│     解析/分块/向量化/图谱抽取/混合检索/重排/知识治理(验证·时效)
│     按 L0 协议直连暴露,entitlement 控制可用性
│
└── 资产层(Knowledge Bases,对应 P-T-A 三级)
      P:平台知识库 — 平台供给,跨 org 只读,entitlement 售卖
      T:org 知识库 — org 管理员建设,org 内按配置可见
      A:agent 知识库 — agent 属主(org × ws × product 键),默认私有,
         经 SharingGrant 授权给其他 WS/product
```

要点:

- **所有权与托管分离**:知识库由 agent / org 拥有(ownership),由 Karda 托管(hosting)——索引态资产(向量索引、图谱实例、分块存储)落在 Karda 基础设施内,检索能力无法脱离索引运行;
- **知识流动由属主发起**:属主建库 → 绑定 Arda DataSource 或直接上传 → Karda 加工;不存在"平台自动拉通汇聚"。agent 运行沉淀的知识(如客服 QA 对)由 agent 用 Karda 能力写入自有库,再按 grant 开放,无需单独的"回流"机制;
- **派生边 day-one 建模(v1.0 硬约束)**:Karda 在 **ingestion 时点**为每个派生资产(分块/向量/图谱条目)记录派生边——`派生资产 → 来源 DataSource/文档 → 授权依据(grant_id 或自有)`。这是级联撤销(§8.6)的前提:派生关系若事后补记,已建索引无派生记录,撤销无从级联。写入 Karda 产品定义硬约束;
- **跨库统一检索为一等能力**:调用方给身份,Karda 在其全部可见范围(自有库 ∪ 被授权库 ∪ org 库 ∪ 已订阅平台库)上做联合检索 + 统一重排——保留 org 级"什么都能问"的体验,语义为"调用方可见范围的并集"而非"平台拉通一切"。可见范围求值复用 §8.5 物化可见集,联合检索扇出与策略求值共用一份物化;
- **授权即隔离**:Karda 无 agent-db 物理兜底,agent 知识间的边界完全由授权逻辑 + 索引命名空间成立,§10 两条硬约束适用;
- **例外条款**:极端敏感或有驻留合规要求的知识,允许 agent 侧申请独立索引域(silo-on-demand 通路),例外审批而非默认选项。

### 5.2 Terra:物理世界能力域 + 标准化底座托管

**定位**:数字孪生 + 基础地理 + 物联感知的横向能力底座,附带标准化空间底座数据的跨租户供给。

- 能力层:时空计算、孪生建模与运行、感知接入等,直连暴露;
- P 级:地图、遥感影像等三方公开服务一律按 **brokered service** 形态供给(§4.2,许可主体在持牌服务商,平台卖接入与额度);通用白膜等结构统一(OGC 等标准背书)且许可清晰的底座数据按 asset 形态供给;两形态均须携带来源与许可合规元数据并受来源审计;
- T/A 级:租户自有空间数据严格隔离;业务层数据(物联感知流、孪生业务实体)结构因业务而异,留在 agent-db,保留比例由各 agent 按业务复杂度自行确定;孪生实例的运行态由 Terra 托管(运行时托管,§4.3 次变量)。

### 5.3 Arda:数据服务平台,通用件托管 + 汇聚共享

**定位**:通用结构化数据 + 数据汇聚共享。agent-db 是数据的 System of Record,Arda 是数据的 System of Access。

- 能力层:数据目录、数据服务化输出、连接与同步调度,直连暴露;
- P 级:跨业务复用的通用参考/主数据(v1 清单与准入流程归 Arda 产品定义,见 §12 决策#4;准入判据对齐 Rule of Two 精神——≥2 个 agent 需要且无租户属性才进 P 级);
- T/A 级:业务数据驻留 agent-db(一产品一套、WS 隔离),Arda 目录层记录 `(org, ws, product, datasource)` 四元组;跨 agent 数据共享经 Arda 授权语义显式声明,默认不共享。

### 5.4 连接器归属:连接 = Arda,理解 = Karda

- **连接器注册与目录归 Arda**。连接器是 DataSource 的类型化实现:① 内部 agent-db(首要类型);② 外部结构化源;③ 外部非结构化源(Notion / 飞书 / SharePoint / 对象存储等)。"平台知道哪里有数据"的唯一登记处是 Arda;
- **非结构化内容的消费管线归 Karda**。Arda 负责连接、鉴权、同步调度;内容经通道交付 Karda 完成解析、分块、向量化、图谱抽取(并按 §5.1 记录派生边)。管线由**属主拉动**(建库时绑定 DataSource),而非平台推动;
- Karda 对 Arda 的依赖为实现层依赖,不上升为矩阵结构概念。

---

## 6. Runos:商业能力面(联邦域保留 + 商业能力域新增)

### 6.1 定位:商业能力面 + 两域模型(v1.1 改写)

**Runos 是商业能力面**——业务场景 agent 除模型推理外的一切非模型能力,均经 Runos 网关消费。Runos 聚合四类原语(**连接器 / 技能 / 执行器 / 资产**),经两段裁决(opera 技术供给 → admin 商业封装)开放为**可售能力包**。**控制面(管理 API)不在调用链路上;数据面(网关 + 执行器沙箱)在商业域调用链路上。**(精确判定见 §6.7。)

Runos 的义务**按域分列**,两域共存不矛盾:

| 维度       | **联邦域** Federation                    | **商业能力域** Commercial                          |
| ---------- | ---------------------------------------- | -------------------------------------------------- |
| 谁         | 六大平台互为对等 + 其域内 agent          | 业务场景 agent(N × agent)                          |
| 流量       | 对等直连(L0 工具协议,§7.1),自域内进程内  | **恒经 Runos 网关**                                |
| Runos 角色 | 对等一员,**无任何义务**,无联邦流量经过它 | 治理面:四方合取 + 凭证注入 + 配额计量 + 全链路审计 |
| 秩序来源   | 协议与约定(归平台工程)                   | 运行时强制                                         |
| 授权求值点 | L2 自入口(grant ∧ entitlement,§8.3)      | Runos 网关(有效 entitlement 快照)                  |

三条边界,防止两域互相渗透:

1. **域归属是商业事实,不是团队事实**:以产品/订阅**独立交付**的 agent 属商业域;随平台产品交付、**无独立订阅**的 agent 属联邦域。归属由 opera 按产品目录登记,agent 不自声明;平台内嵌 agent 的某功能后续若需按 tier 定价,做法是把该流量**迁入商业域**,而不是把商业逻辑加到联邦侧;
2. **同一底层能力可同时存在于两域**:联邦域内 agent 直连消费(L2 自有产品面)保持不变,商业域内作为可售 SKU 经网关消费——**一套实现,两道门**(§7.2);
3. **联邦域 = v1.0 原模型,原样保留**:本节以下各款凡涉及"链路/授权/执行/计量"的结论,一律**按域读**;未标注域者为两域皆真。

### 6.2 Skill 构成

```
Skill = 指令/Prompt 模板 + 编排逻辑与少样本示例
      + 专用模型绑定(分类器/抽取器/NER/OCR/rerank 微调版等,经 Atlas 调用)
      + 工具引用(引用 L1/L2 一方工具,如 Karda 检索、Terra 地理编码)
      + 元数据(版本、评测基线、entitlement/grant 声明)
```

技能资产同样遵循 P-T-A 三级供给:平台技能(跨 tenant,entitlement 消费)/ org 技能(租户自建,grant 共享)/ agent 私有技能——Runos 亦为 §4 统一原型的实例,其"资产"是技能而非数据。

**技能(Skill)是商业域四原语之一**(v1.1):四原语的完整定义、注册与授权模型归 runos 自有设计(`vxture-runos` `docs/30-design/100-overview.md` §3),本文只固化平台侧边界——**资产原语只登记元数据与授权引用,内容留在来源系统**(karda 知识库、ontos 本体等),与 §8"共享不产生数据副本"同源;**技能仍在 agent 运行时由模型解释执行**,网关只观测其内容取用,技能的调用事件是**分发事件而非执行度量**,其执行成本(模型 token)恒在 Atlas 账上。

### 6.3 Runos 的职责(控制面四项 + 商业域数据面三项,v1.1 收窄)

**控制面四项(两域共有,不在调用链路上)**:

1. **技能定义与生命周期**:创建、版本、评测基线(eval)、发布与废弃。**eval 的执行不在 Runos**:评测集与基线定义、结果记录持有在 Runos;评测执行按 §6.6 构件就位模型走——推理在 Atlas、编排执行在 L0 沙箱(评测作业),结果回写 Runos 目录;
2. **技能目录与分发**:agent 按 entitlement/grant 发现并装载可用技能;分发为**拉取式**(agent 启动或按需拉取技能定义与签名构件),非推送式常驻通道;
3. **专用小模型的能力化**:任务层包装与目录登记(如"抽取模型 + Prompt 套件 = 合同要素抽取技能");**模型接入、推理与计量本身归 Atlas**,推理流量不经过 Runos;
4. **跨域组合**:将多个域的原子能力组合为任务能力(如"合规审查技能 = Karda 法规检索 + 条款抽取小模型 + 比对 Prompt")——单一 L2 做不了、agent 各自做会重复,是 Runos 的价值锚点。

**商业域数据面三项(v1.1 新增,仅商业域)**:

5. **网关承载调用**:authn(用户 → agent 实例 → 能力三级主体链,身份恒由平台 IdP 签发,网关只校验不签发)、凭证注入、配额执行、沙箱隔离、全链路审计;
6. **有效 entitlement 快照**:两段裁决(opera 技术供给上限 → admin 商业封装,配额 = min(技术上限, 商业配额))编译为快照下发,网关判定 = **四方合取**——已供给 ∧ 该 tier 已授权 ∧ 配额未超 ∧ 策略允许;
7. **执行器作为原语并在 Runos 自有沙箱运行**:执行器沙箱是**基础设施**(自有镜像供应链、隔离模型、独立部署件、水平扩展),与 §6.6 的 L0 共享沙箱是两件东西。

数据面**不持有权威状态**(仅可失效的本地快照),控制面故障不阻断在途调用;控制面(管理 API)在两域皆不在调用链路上。

### 6.4 Runos 不是什么(负面清单,v1.1 按域收窄)

**保留不变(两域皆真)**:

- 不是 L2 的 API 网关:**L2 原生直连面不经 Runos 转发**——商业域内经网关消费的是 L2 主动登记为能力原语的端点,不是 L2 原生面的第二接口(§7.2);
- 不是模型平台:模型接入/路由/配额/推理宿主归 Atlas(含大模型与专用小模型),**推理流量恒不经 Runos**;
- 不是 agent 的工作流引擎:agent 内部业务编排归 agent 自身,Runos 只提供可被编排的"件"(含 `subscription` 类能力——事件送达归 Runos,送达后的唤醒与调度归 agent 运行时)。

**按域收窄**:

- "不出现在任何调用链路上" → **联邦域**不出现(无中心瓶颈,无 ESB 反模式);**商业域**网关即单一能力门,业务场景 agent 不得绕过;
- "不是唯一授权点" → **联邦域**授权在 L2 自入口求值;**商业域**授权在网关(有效 entitlement 快照,四方合取)。

### 6.4a 商业域授权分两层,Runos 只管第一层(owner 裁定 2026-08-10)

商业域的"授权"是**两件不同的事**,先前的写法把它们压成了一件,于是 Runos 看起来要为数据对象的可见性负责——不对。

| 层             | 判什么                                | 谁判                     | 依据                                             |
| -------------- | ------------------------------------- | ------------------------ | ------------------------------------------------ |
| **① 能力授权** | 这个调用方能不能用**这个能力**        | **Runos 网关**(四方合取) | 能力随 **agent 产品档位**绑定下发(见下)          |
| **② 对象授权** | 这次调用能不能碰**这份数据/这个对象** | **调用方 agent**         | agent 自己的权限模型 + §8 grant / 提供方入口求值 |

**Runos 在第二层只执行,不裁决。** 技能/连接器去取数据时,"这个用户能不能看这份数据"由 agent 判断——agent 才持有用户上下文、才知道自己的业务权限模型。Runos 若也去判,就是把同一件事判两遍、且两处必然漂移。

**MVP 姿态(owner 2026-08-10 定)**:**先用起来,不先管起来。** 第一层按档位派生下发即可,**不为 M1 另建任何授权机制**;第二层本来就不该平台去建——它是 agent 自己的判断。宁可初期放宽,也不要为了"管住"而先造一套没有真实调用方在用的门。

**第一层的绑定是派生的,不是单卖的**:客户买的是 **agent 产品的档位**,该档位打包了哪些 Runos 能力,客户就自动能用哪些——**没有"Runos 订阅"这种东西**,与 Atlas 同形(能力作为 `bundled` 组件计入宿主产品定价,`product_220`)。

**谁是调用方,这里必须说死**:**L3 agent**。`L0/L1/L2`(平台本体 / atlas·ontos·runos / arda·karda·terra·ontos)是**平台架构层**——它们向 Runos **供给**能力,不是买能力的消费方,与 agent 有本质区别。推论:商业域的调用方天然携带客户订阅,**Runos 自己永远不是调用方**,也就不需要任何"Runos 对某 workspace 有覆盖"的授予。

### 6.5 技能准入判据(Rule of Two)

一段任务逻辑仅一个 agent 使用 → 留在该 agent 内部;被两个及以上 agent 复用,或需作为平台技能 SKU 售卖 → 沉淀为 Runos 技能。可操作化为三问:是否跨域组合?是否绑定专用模型?是否包含值得复用的任务逻辑?——三者有其一进 Runos,否则不进。

### 6.6 执行位置模型:联邦域构件分布式就位 + 商业域网关承载

**联邦域(v1.0 原文,原样有效)**——技能执行不是"在 Runos 跑还是在 agent 跑"的二选一;Skill 的每类构件按各自天然归属就位执行:

| Skill 构件                             | 执行位置                                                                  | 行业对应                                                  |
| -------------------------------------- | ------------------------------------------------------------------------- | --------------------------------------------------------- |
| 指令/Prompt/编排逻辑(声明性部分)       | **agent 运行时**:技能定义分发装载进 agent 上下文,由 agent 的 LLM 循环消费 | Anthropic Skills、OpenClaw(技能库分发定义,agent 自行执行) |
| 工具引用                               | **提供方服务**:以调用方 agent 身份直连 L1/L2 入口执行                     | MCP(协议统一,provider 各跑各的 server)                    |
| 专用小模型推理                         | **Atlas 模型端点**:小模型注册为 Atlas 管理的专用模型,与大模型同路         | Bedrock 自定义模型 / SageMaker Endpoint                   |
| 重型非模型计算(OCR 流水线、代码执行等) | **L0 共享沙箱执行器**(平台级基础设施,非 Runos 产品能力)                   | 各家 code interpreter / sandbox 模式                      |

**小模型宿主为 Atlas 的三点理由**(已固化):① Atlas 的产品定义即模型接入/路由/配额/用量治理,专用小模型只是模型子类,不建第二个模型平台;② 租户计量口径必须唯一,推理量不过 Atlas 则计费体系分裂;③ GPU 资源池、弹性伸缩、模型版本管理等重资产运维能力平台只建一套。

边界情况:技能需"作为单个工具被一次性原子调用"时,由 L0 沙箱按技能定义解释执行,或由技能的主要提供域承载——**联邦域内** Runos 仍不在调用链路上。

推论(**联邦域结论,v1.1 收窄**):联邦域内不存在"Runos 托管执行器"选项;Runos 在联邦域无推理与执行负载,对该域为纯元数据服务。

**商业能力域(v1.1 新增)**——执行位置按原语分列,网关在链路上但**不代执行**:

| 商业域原语 | 执行位置                           | 网关观测到什么                                           |
| ---------- | ---------------------------------- | -------------------------------------------------------- |
| 连接器     | **提供方服务**(L2 / 三方 MCP 端点) | 整个往返(请求/响应/时延/错误)                            |
| 执行器     | **Runos 自有沙箱**(基础设施)       | 完整执行过程(供给方与裁判同体,故执行器与三方能力同门槛)  |
| 技能       | **agent 运行时**,由模型解释执行    | 仅内容取用(分发事件,非执行度量;成本=模型 token,在 Atlas) |
| 资产       | 无执行(静态内容被消费)             | 授权与引用解析;内容留在来源系统                          |
| 模型推理   | **Atlas**,恒不经网关               | 无                                                       |

故"Runos 无推理与执行负载、为纯元数据服务、SaaS 成本模型极轻"**仅对联邦域成立**;商业域的网关与执行器沙箱是**延迟敏感、水平扩展、可用性敏感**的重资产,自有镜像供应链(runos `ADR-003` 后果条款)。

### 6.7 控制面 / 数据面精确判定(v1.0 判定表,v1.1 按域分列)

逐生命周期环节判定 Runos 的职责边界。**分列的目的**:防止"无运行时"被读成全局结论,也防止商业域的网关义务渗回联邦域。

| 生命周期环节    | Runos 做什么(控制面)                                   | 联邦域:执行/负载在哪                                                         | 商业域:执行/负载在哪                                                                                       |
| --------------- | ------------------------------------------------------ | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| 创作 / 版本     | 能力定义 CRUD、版本树与别名、构件签名、准入分级        | —(纯元数据)                                                                  | 同左(控制面不在链路)                                                                                       |
| 评测(eval)/质量 | 持有评测集定义、基线、历史结果与质量分                 | 推理 = Atlas;评测作业编排执行 = L0 沙箱;结果回写 Runos                       | 同左;**质量分是商业域样本陈述**(仅网关流量),不覆盖联邦侧流量,亦不代表端点物理负载                          |
| 发布 / 分发     | 目录登记、entitlement/grant 声明、拉取式分发接口       | agent 运行时拉取并装载                                                       | 经网关发现(discover/resolve),按有效 entitlement 快照过滤:未授权能力**不可见**而非被拒                      |
| 执行            | 联邦域 **无**;商业域**网关在链路上**(自身不代执行业务) | 声明部分 = agent LLM 循环;工具引用 = L2 直连;小模型 = Atlas;重计算 = L0 沙箱 | 网关承载调用 → 连接器落提供方端点、执行器落 Runos 沙箱、技能仍在 agent 运行时、推理恒直达 Atlas(§6.6 下半) |
| 授权            | 编译有效 entitlement 快照并下发                        | **不参与**:L2 自入口求值 grant ∧ entitlement(§8.3)                           | **四方合取**:已供给 ∧ tier 已授权 ∧ 配额未超 ∧ 策略允许                                                    |
| 计量            | 联邦域 **零计量路径**(技能"使用数"仅目录统计,不作计费) | 推理量计给 Atlas → consume;工具调用计给提供方 L2 → consume                   | **有计量路径**:配额执行(最终一致软执行)、每调用成本、计费维度;账务对账 SoR = Runos 审计事件流(归属见 §6.8) |
| 废弃 / 撤销     | 目录状态变更(deprecated/revoked);快照失效推送          | agent 下次拉取生效;**不热杀运行中会话**(装载态技能随会话自然结束)            | 快照失效推送:**升级秒级生效、降级给宽限**(在途调用按起始 entitlement 完成)                                 |

版本一致性约定:**联邦域** agent 装载技能采用**版本 pinning**(锁定至具体版本,升级为显式动作),避免技能定义静默漂移改变 agent 行为;技能定义构件带签名,agent 运行时装载前验签。**商业域** agent 消费 `stable` 等版本别名,版本晋升在 Registry 生命周期内经回归门完成,**不构成 entitlement 事件**;别名解析与签名验证义务不变。

### 6.8 商业域未决项(v1.1 新增,平台侧登记)

改写采纳了两域模型,但有三处平台侧口径 runos 现设计未覆盖或未裁定。**登记在此,不阻塞改写落地**;"未裁定前的默认读法"是平台侧按既有不变量的推导,不是终局裁定。

| #   | 未决项                                                                                                 | 未裁定前的默认读法                                                                                                                                                                                                  | 收口时点                               |
| --- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| 1   | **计量归属与去重**:商业域一次调用会同时被网关(能力包口径,计费维度)与提供方 L2(供给侧 consume 口径)记录 | 两条记录是**同一次调用的两个商业口径**(能力包售卖方 vs 供给方),不自动构成重复计费;平台 consume 的上报归属、去重键与账单呈现规则待 runos × 提供方 L2 × billing 三方裁定                                              | **商业域首个可售能力包上线前**必须收口 |
| 2   | ~~org 内 SharingGrant 在商业域的求值点~~ → **已裁定 2026-08-10**                                       | **对象授权归调用方 agent,不归 Runos**(§6.4a):Runos 判"能不能用这个能力"(第一层),"能不能碰这份数据"由 agent 按自己的权限模型判、Runos 只执行。所以四方合取不含 grant **是正确的,不是缺口**——grant 求值本就不在网关上 | ✅ 收口                                |
| 3   | 模型推理口径                                                                                           | **不变且不可协商**:推理量恒计给 Atlas,技能执行的模型 token 在 Atlas 账上,不在 Runos;Runos 自身的模型需求(语义发现 embedding、质量评判)以**普通 Atlas 调用方**身份计量                                               | 已固化,无需再议                        |

---

## 7. 能力供给:唯一直连通道

### 7.1 L0 工具协议规范(已固化)

能力以什么统一接口形态暴露(工具 schema、调用规范、鉴权与身份透传约定)是 **L0 级平台技术标准**(MCP 风格):由 L0 定义规范文档,各产品(L1/L2)负责实现。协议统一 ≠ 物理网关——不设中心代理节点。

> 规范文档为独立待建项(族内编号 `product_210_tool-protocol.md`,登记于 [`product_200_integration.md`](./product_200_integration.md) §5):须覆盖工具 schema 约定、**S2S 鉴权与调用方身份透传**(token exchange / on-behalf-of,现有接入标准只覆盖用户级 OIDC)、grant ∧ entitlement 求值时点、审计与计量归属。这是本模型落地的最大工程前置。

### 7.2 唯一供给通道:直连(联邦域,已固化)

**联邦域内,L2 的能力供给通道只有一条——直连。Runos 不构成第二条供给通道。**

- L2 为独立产品,须有产品内的完整体现:自有 Console 体验、自有 API/工具面、自有入口授权求值;能力对 agent 的暴露是产品完整性的一部分;
- 若在**联邦域内**设"经 Runos 访问 L2"的第二通道,将产生:同一能力双接口面(版本演进双处同步)、授权双重求值(语义可能不一致)、L2 产品完整性受损(用量与体验数据旁落)——**必要性分析结论:不必要,且有害,明确不做**;
- **Runos 在联邦域位于消费侧而非供给侧**:它供给的是"引用了 L2 能力的技能";技能运行时对 L2 的每次调用,仍以调用方 agent 身份走唯一直连通道,权限链路、计量归属、版本契约全部落在 L2 自己的入口。

**商业能力域是第二个域,不是第二个接口(v1.1 增补)**:商业域的消费者是**另一群体**(以订阅独立交付的业务场景 agent),消费的是**可售能力包**,通道恒为 Runos 网关。这不与上文冲突,理由三条:

- **L2 主动登记,而非被代理**:L2 把选择商业化的能力**登记**为连接器/资产原语,登记的是端点与元数据;L2 的原生直连面、入口求值、Console 体验、产品完整性一概不变,Runos 不为其另开一套接口面;
- **一套实现,两道门**:同一端点可同时服务联邦直连流量与商业网关流量;Runos 只观测并强制其中**商业域的那一半**——opera 设的技术上限是**商业域配额**,不是端点物理容量,Runos 不保护也不假装保护端点免受联邦侧负载;
- **v1.0 反对的第二通道是"同一群消费者的双接口面"**(双向版本同步、双重求值语义分叉、用量与体验数据旁落),该反对在联邦域内**继续成立**;商业域是不同消费者、不同商业事实下的不同消费路径,不产生上述三种损害。

### 7.3 供给结构总图(v1.1 按域分列)

**联邦域(原样保留)**:

```
L2(Karda/Terra/Arda/Ontos) ══唯一直连通道(L0 协议 + 各自入口 grant∧entitlement 求值)══→ 域内 agent
                                     ↑ 工具引用(以 agent 身份调用)
Runos(能力资产:定义/版本/评测/分发,控制面) ──分发──→ agent 运行时装载(无联邦流量经 Runos)
Atlas ──模型推理(大模型 + 技能绑定的专用小模型,统一计量)──→ agent / 技能执行
Ontos(实体/关系/语义 Schema 定义) ──被 L2 消费(Schema 归 Ontos,实例归各 L2)
L0 vxture:org/WS/entitlement/计量计费/工具协议规范/共享沙箱/sharing 策略 SoT 贯穿全部
Terra P 级底座(地图/影像=brokered,白膜=asset) ──跨 tenant 只读──→ 各 org(entitlement 消费)
```

**商业能力域(v1.1 新增)**:

```
供给方(L2 / 三方 / Runos 自有执行器) ──登记四原语(连接器/技能/执行器/资产)──→ Runos 目录
                                          ↓ 两段裁决:opera 技术供给上限 → admin 商业封装(能力包 × tier)
                                    有效 entitlement 快照 ──下发──→ 网关(数据面,不持权威状态)
业务场景 agent ══恒经 Runos 网关(四方合取 + 凭证注入 + 配额计量 + 全链路审计)══→ 提供方端点 / Runos 沙箱
Atlas ──模型推理恒不经网关(Runos 亦为普通调用方)──→ 业务场景 agent
同一提供方端点:联邦直连 + 商业网关 = 一套实现两道门(Runos 只覆盖商业那一半)
```

---

## 8. 共享策略模型(SharingGrant)

### 8.1 最小策略模型

```
SharingGrant {
  resource:    数据集 / 知识库 / 技能(归属键 = org + ws + product,资产 id 为跨面 loose 引用)
  grantee:     org 内的某 WS | 某 product | org-all
  scope:       按 resource 类型参数化(§8.2),区分"可召回"与"可应用"两级敏感度
  status:      active / revoked
  expires_at:  可选到期时间(临时协作/外审场景;到期即失效,与撤销是两条独立轴)
}
```

### 8.2 scope 按资产类型参数化(v1.0 细化)

| resource 类型(资产面) | scope 值域           | 语义                                                                                                                    |
| --------------------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 数据集(Arda)          | `read`               | 可经 Arda 服务面查询/读取(不含写;写永远只属属主)                                                                        |
| 知识库(Karda)         | `retrieve` < `apply` | `retrieve` = 允许被检索召回、生成时**必须带出处引用**;`apply` = 允许用于生成与应用(无引用生成)。`apply` 蕴含 `retrieve` |
| 技能(Runos)           | `use`                | 可被 grantee 发现、装载并执行                                                                                           |

### 8.3 命中谓词与联合求值(v1.0 写死,防实现分叉)

```
grant 命中(caller, resource) :=
      status = 'active'
  ∧  (expires_at IS NULL ∨ now() < expires_at)
  ∧  (   grantee = org-all
      ∨ (grantee = WS      ∧ caller.workspace_id = grantee.workspace_id)
      ∨ (grantee = product ∧ caller.product_id   = grantee.product_id) )

可访问(caller, resource) :=
      属主访问(caller.ws = resource.ws ∧ caller.product = resource.product)   -- 自有资产无需 grant
   ∨ (∃ 命中 grant ∧ entitlement_active(caller.workspace_id, 执行点产品))
```

- **grantee = product 的精确语义**:本 org 内、经由该 product 访问的**任意 WS 实例**(与 entitlement 的 workspace × product 双轴对齐,联合求值中的 entitlement 项继续约束具体 WS);
- **联合求值的 entitlement 操作数 = 执行点产品**(承载/服务该资产的 L1/L2,如知识库之于 Karda):调用方 WS 须持有执行点产品的有效 entitlement;**资产归属产品的订阅不作要求**(共享即授权),属主性由归属键保证;
- **多 grant 合成**:同一 (resource, caller) 命中多条 grant 时,scope **就高合成**(各条先独立判定 status/expires,有效者取最强 scope);不同 resource 之间互不影响;
- **撤销与重授**:撤销 = status 置 `revoked`(行保留,审计需要);重新授权 = 新建 grant 行,不复活旧行;
- **T 级 org 库的可见性同样以 grant 表达**(org-all 预设 grant),三级可见性(org 共享 / WS 共享 / agent 专属)是 grant 预设形式,不是数据的静态属性;
- **P 级资产不走 grant**:跨 tenant 的平台资产供给走 entitlement(§4.2),grant 仅为 org 内机制。

### 8.4 设计要点(承 v0.2,继续有效)

1. **默认拒绝(default-deny)**:无 grant 即不可见;
2. **策略执行点在各资产面入口**:Arda(数据)、Karda(知识)在各自服务入口执行 grant 求值;Runos 仅对技能资产自身执行;**权限过滤在召回层强制执行,不做生成后裁剪**;
3. **默认值差异**:数据默认私有、显式共享;知识默认按作用域声明——数据敏感度高于知识;
4. **可信度元数据随共享透出**(可选属性):跨 WS 消费时,资源的验证/时效状态作为元数据透出(非新 scope),服务受监管场景。

### 8.5 策略 SoT 与物化(v1.0 落位,owner 拍板)

**SoT = 平台控制面新增 `sharing` 域(schema)。** 理由:grant 横贯数据/知识/技能三类资产、跨 L2 产品,任何单个 L2 都不是自然属主;联合求值 grant ∧ entitlement 的两个操作数同源(控制面)才能一处求值、一处物化、一处失效;org 管理员全局审计与一键回收天然是平台 Console/Admin 界面;审计走现成 `support.audit_log`。

```
sharing.grants                    -- SoT
  id / tenant_id(FK,org 内机制硬约束)
  resource_type(dataset|knowledge_base|skill)
  resource_product_id(FK) / resource_workspace_id(FK)
  resource_ref                    -- 业务面资产 id,跨面 loose 引用(铁律一边界#1)
  grantee_type(workspace|product|org_all) + grantee_workspace_id? / grantee_product_id?
  scope / status(active|revoked) / expires_at?
  created_by / revoked_at / revoked_by

sharing.visible_set_current       -- 物化可见集(非 SoT,对齐 entitlement_current 模式)
  (grantee 键, resource 键, scope) UPSERT + 短 TTL
```

- **求值执行点不变**(仍在各 L2 入口,召回层强制);控制面提供解析 API + 按 grantee 预展开的物化可见集 + **invalidate 推送**(grant 变更秒级生效,复用 entitlement invalidate 通道,见对接标准 C3);
- 字段级 DDL 归后续 `data_sharing_100/200` 设计线(命名遵循 `data_{domain}_{NNN}` 规则);
- Karda 跨库统一检索(§5.1)与策略求值共用同一份物化可见集,检索扇出不重复求值。

### 8.6 级联撤销(lineage-aware revocation,v1.0 机制化)

覆盖"数据已撤销、派生知识索引仍可召回"风险(§10#2):

1. **派生边在 ingestion 时点记录**(§5.1 硬约束):`派生资产 → 来源 DataSource/文档 → 授权依据(grant_id | 自有)`,存于 Karda/Arda 业务面元数据;
2. **撤销事件广播**:grant 撤销/到期、DataSource 解绑在控制面产生事件,经 invalidate 通道推送相关 L2;
3. **re-scope 而非 re-index**:L2 按派生边将受影响的派生资产移出可见集(索引条目失效标记),不重建索引;召回层过滤保证即时不可见;
4. 审计:撤销事件与受影响资产计数写审计,供合规追溯。

### 8.7 管理权模型(v1.0 已固化,原 §8.3 建议转正)

**WS 发起 + org 审计回收**:grant 由资源所属 WS 的管理员发起;org 管理员保留全局审计与**一键回收**权;资源属主 WS 可随时撤销自己发出的 grant。全部 grant 创建/撤销/到期事件写 `support.audit_log`(actor / resource / grantee / scope)。

---

## 9. 行业对标结论(摘要)

- **隔离**:org 硬隔离 + 共享 Schema 键隔离对齐 Salesforce 经典多租户;org → 多 WS 结构对齐 Slack Enterprise Grid;相对 Glean 单租户部署更轻,以 per-tenant 加密(BYOK)+ 数据驻留选项(高阶 tier)+ silo-on-demand 预留通路作为补偿演进;
- **共享**:"共享不复制"对齐 Snowflake Secure Data Sharing;"策略执行点收敛于资产面"对齐 Databricks Unity Catalog;"召回层执行权限"对齐 Glean permission-aware retrieval;grant 到期语义对齐 Databricks sharing expiration;**grant 语义统一横贯数据/知识/技能是超出行业常规的部分**——这正是策略 SoT 必须单一(§8.5)的原因;
- **能力**:"能力同构、配置分化"对齐 Salesforce 单平台内核模式;"底座供养 agent"对齐 Glean / Agentforce;与 Dify/Coze 的"agent 私有为默认"取相反默认值,以避免知识碎片化;Runos 的分布式执行模型对齐 MCP + Anthropic Skills 生态,规避 ESB 反模式;
- **叙事差异点**:比 Glean 多出 Ontos 语义面与 Arda 数据面的显式建模,且以 L2 统一原型(P-T-A + 托管水位线)将其结构化。

---

## 10. 技术约束与风险应对

| #   | 风险                       | 说明                                                                                             | 应对(v1.0 更新)                                                                                                                                                                                                                                      |
| --- | -------------------------- | ------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 策略求值性能               | 每次检索需 grant 求交,延迟随 grant 数量线性恶化                                                  | **已设计**:§8.5 物化可见集(按 grantee 预展开)+ 短 TTL + invalidate;检索与策略求值共用物化                                                                                                                                                            |
| 2   | 跨面策略一致性             | 数据(Arda)与知识(Karda)共用 grant 语义,若无单一策略存储,会出现"数据已撤销、派生知识索引仍可召回" | **已落位**:单一策略 SoT = 控制面 `sharing` 域(§8.5);级联撤销机制化(§8.6,派生边 day-one + 事件广播 + re-scope)                                                                                                                                        |
| 3   | default-deny 冷启动        | 过严默认导致 agent"什么都查不到"                                                                 | WS 创建时提供共享策略模板(grant 预设),安全默认与体验默认分开处理;对齐 WS onboarding 流程                                                                                                                                                             |
| 4   | 向量索引隔离(Karda 硬约束) | Karda 全量托管、无 agent-db 物理兜底,共享向量库 + 元数据过滤易因过滤失效跨租户泄漏               | **org 级逻辑上强制、物理上分档**:大 org 独立 collection/namespace(物理隔离),小 org 可共享物理 collection + 引擎级强制 org_id 过滤(RLS/payload 过滤),分档阈值运营可调;WS/agent 级用元数据过滤;召回层强制权限,无生成后裁剪;silo-on-demand 例外通路保留 |
| 5   | P 级资产许可合规           | 地图/影像等外部许可数据跨 tenant 再分发的条款风险                                                | **形态二分**(§4.2):管制类走 brokered service(许可主体在服务商);asset 类元数据强制含来源/许可字段;**全生命周期来源审计**(准入登记/版本变更/分发消费/许可复审);敏感数据红线(不进 P 级)                                                                 |

---

## 11. 边界之外(不做的事)

- Karda 不做租户/订阅/计费/entitlement(归 L0);不做模型管理与路由(归 Atlas);不做本体 Schema 定义(归 Ontos);不做行业垂直 Agent 应用(归 L3);
- Runos 不做 L2 原生面的转发网关、不做模型平台、不做 agent 工作流引擎(三条两域皆真);**联邦域**内不设托管执行器、不出现在调用链路、零计量;**商业域**网关在调用链路上、执行器沙箱自有、有配额与计量——按域读,精确判定 §6.7;
- Atlas 之外不存在第二个模型宿主(含专用小模型);
- L2 前置订阅约束归各 L3 产品自身订阅规则配置;
- **umbra(边界 VPN,域名 ruyin.ai,即原目录中的"ruyin"对接方)不进入本模型**:保持现状租户级订阅模式与 OIDC RP 契约(豁免 workspace × product 权益引擎与 SharingGrant),见 [`product_100_matrix.md`](./product_100_matrix.md) §2/§3;code 改名分阶段规划 = [`product_300_naming-migration.md`](./product_300_naming-migration.md) §2(本次不实施);
- **Ruyin(重新定义:client 端产品,desktop)不共享 entitlement 模型**,Karda 知识库能力默认不对 Ruyin 开放(仅 Atlas/Runos 层能力互通);产品定义待建;
- L0 共享沙箱本阶段仅文档占位,不实施(起步最小化);
- 不建中心网关/ESB;不在平台控制面建向量/索引表(Karda 索引在其产品数据面)。

---

## 12. 决策记录(原"待拍板项",2026-07-06 全部拍板)

| #   | 决策项                            | 结论                                                                                                                                                 | 备注                                                                                |
| --- | --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| 1   | 图谱实例归属                      | ✅ **Schema 归 Ontos、实例归 Karda**(实例量大、随知识域生命周期、需 WS 隔离)                                                                         | v0.2 倾向确认                                                                       |
| 2   | 共享管理权                        | ✅ **WS 发起 + org 全局审计与一键回收**,转已固化(§8.7)                                                                                               | v0.2 倾向确认                                                                       |
| 3   | Arda/Karda 边界案例裁定规则       | ✅ 归 **Karda 产品定义**给出裁定规则;起步判据 = **按消费方式裁定**(被检索/引用消费的进知识域,被查询/计算消费的留数据域;同一载体可双注册)             | 移交 Karda 产品定义(待建),保持跟踪                                                  |
| 4   | Arda P 级通用数据范围清单         | ✅ 归 **Arda 产品定义**列 v1 清单并定义准入流程;准入判据 = Rule of Two 精神(≥2 agent 需要且无租户属性)                                               | 移交 Arda 产品定义(待建),保持跟踪                                                   |
| 5   | 产品名称(评审新增)                | ✅ **全部采纳为终版名称,稳定使用**(除非再次修订):矩阵与映射见 [`product_100_matrix.md`](./product_100_matrix.md);销 `data_platform_300` §18.2#5 待决 | owner 拍板                                                                          |
| 6   | ruyin/umbra(评审新增)             | ✅ 现 ruyin.ai 对接方 = **umbra(边界 VPN)**,域名不变,**保持现状订阅模式**(原 ruyin 的租户级订阅);**Ruyin 重新定义为 client 端产品(desktop)**         | owner 拍板;code 改名规划 = product_300 §2,**本次不实施**(owner 2026-07-06 二次确认) |
| 7   | SharingGrant 策略 SoT(评审新增)   | ✅ **平台控制面新增 `sharing` 域**(§8.5),物化 + invalidate 复用 entitlement 模式                                                                     | owner 拍板                                                                          |
| 8   | P 级供给形态(评审新增)            | ✅ **二分 asset / brokered service**;P 级范围 = 行业公开数据 + 三方公开服务数据,敏感数据不进平台;**来源审计强化**                                    | owner 拍板                                                                          |
| 9   | 上游文档(评审新增)                | ✅ 外部 product-matrix v1.1 / ADR-entitlement-and-workspace v2 **不再提供,由本仓重构取代**:`product_100_matrix.md` v1.0 + ADR-11(既有)+ ADR-12(新增) | owner 拍板                                                                          |
| 10  | 业务面模板 workspace 化(评审新增) | ✅ 更新 `data_platform_100` §2.3 业务面模板与契约(workspace_id 隔离键 + L2 域平台条款)                                                               | owner 拍板                                                                          |
| 11  | superseded 文档处置(评审新增)     | ✅ 被取代文档**标记删除**(banner 标注,非删除操作),重构完成后再执行删除                                                                               | owner 拍板                                                                          |
| 12  | Runos 两域模型(v1.1 新增)         | ✅ **采纳**:联邦域原样保留;新增商业能力域,Runos = 商业能力面,网关在**商业域**调用链路上;§6/§7 按域收窄,非反转                                        | owner 决定 2026-08-07(runos `ADR-003`);平台侧办理 = issue #216;未决项见 §6.8        |

---

## 13. 已固化项(本文档确认,无需再议)

- **L2 统一原型**:能力层 + P-T-A 三级资产层 + 授权层;三个 L2 为同一模式的参数化实例,主变量为托管水位线(由数据结构收敛度决定),次变量为运行时托管;
- **托管水位线取值**:Karda 全量托管(agent 基本不需自建知识 db)/ Terra 托管标准化底座(业务数据留 agent-db,比例自定)/ Arda 托管通用件 + 汇聚共享;
- **P 级资产**:范围 = 行业公开数据 + 三方公开服务数据(敏感数据不进平台);形态二分 asset / brokered service;经 entitlement 消费不走 grant;全生命周期来源审计;
- **连接 = Arda,理解 = Karda**;知识管线由属主拉动;知识沉淀直接写入自有库,无独立"回流"机制;**派生边 ingestion 时点记录**(级联撤销前提);
- **跨库统一检索**为 Karda 一等能力,语义为"调用方可见范围的并集",与策略求值共用物化可见集;
- **工具协议规范归 L0**:平台技术标准,各产品实现,不设物理网关;含 S2S 身份透传(规范文档待建,最大工程前置);
- **联邦域内 L2 供给通道唯一为直连**;Runos 在该域位于消费侧,不构成第二通道;**商业能力域**是不同消费者群体消费可售能力包的第二个域,经 Runos 网关,不为 L2 原生面另开接口(§7.2);
- **Runos = 商业能力面**(v1.1):控制面(管理 API)两域皆不在调用链路;**联邦域**无运行时、零计量;**商业域**数据面(网关 + 执行器沙箱)在调用链路上,四方合取授权、有配额与计量(§6.7 判定表);能力准入遵循 Rule of Two;技能装载版本 pinning + 构件签名;
- **技能执行按构件分布式就位(联邦域)**:声明部分在 agent 运行时、工具引用直连提供方、小模型推理在 Atlas、重计算在 L0 沙箱;eval 执行同此(Runos 只存定义与结果);**商业域**执行位置按原语分列,执行器落 Runos 自有沙箱(§6.6 下半);
- **小模型宿主为 Atlas**(计量口径唯一、模型平台唯一、重资产运维唯一);
- **共享不产生数据副本**;权限过滤在召回层强制执行;org 为绝对隔离边界,P 级资产为唯一合法跨 tenant 形态;
- **SharingGrant**:SoT 在平台控制面 `sharing` 域;命中谓词与联合求值按 §8.3;scope 按资产类型参数化;多 grant 就高合成;撤销保留行、重授新行;管理权 = WS 发起 + org 审计回收;
- 沿用既定:entitlement 粒度 workspace × product(state × tier);L0 vxture 不作为 product code;Ruyin(client 端)/umbra 不进 entitlement 新引擎(umbra 保持现状订阅);agent-db 一产品一套、按 WS 隔离。

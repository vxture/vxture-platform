# 95-readme — 包使用文档归集

内部包（private，非发布）的使用/API 参考文档统一归集于此（原散落各 package 的 `README.md`，2026-07-17 收敛）。
**已发布包**（`@vxture/shared`、`@vxture/design-system`）的 README 保留在 package（作 npm registry 页面）。

> 归集为第一步（先集中）；后续可与 [`../40-implementation/packages/`](../40-implementation/packages/00-index.md) 的实现文档合并去重。
> 每 package 只保留一个 `AGENTS.md` 作为入口指针（指向 `docs/`）。命名规范见 [`../10-standards/070-docs-taxonomy.md`](../10-standards/070-docs-taxonomy.md)。

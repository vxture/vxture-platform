# GitHub Packages 鉴权（拆仓前置，vxture-platform#批2）。
#
# ── 为什么必须有这个文件 ──
# 设计三包拆到 vxture/vxture-design 之后，@vxture/* 不再是 workspace:* 而是从
# registry 装。**GitHub Packages 即便包是公开的也要求鉴权**——没有这两行，
# `pnpm install` 会去 npmjs.org 找 @vxture/design-* 然后 404。
#
# ── token 从环境变量取，不落盘 ──
# CI 由 setup-node-pnpm 注入 NODE_AUTH_TOKEN；Docker 构建走 BuildKit secret
# （见 deploy/docker/Dockerfile.*，**不能用 ARG**，ARG 会留在镜像层历史里）；
# 本地开发者自己 export，或用 `gh auth token`。
#
# 变量未设时 pnpm 会以空 token 请求并得到 401——那是清楚的失败。相比之下
# 把 token 写进这个文件会被提交上去，是不可逆的泄露。
@vxture:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}

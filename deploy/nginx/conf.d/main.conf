# conf.d/main.conf — 把各 server 块引入 http{} 上下文。
#
# stock 镜像 nginx.conf 在 http{} 内有 `include /etc/nginx/conf.d/*.conf;`,
# 故本目录下的 *.conf 均在 http{} 级生效。本文件仅负责挂载 sites-enabled。
# (此前该文件是手工放在主机上的,现纳入仓库统一管理,消除配置漂移。)
include /etc/nginx/sites-enabled/*.conf;

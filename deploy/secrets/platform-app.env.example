# ═══════════════════════════════════════════════════════════════════════════
# platform-app.env — 应用运行时 DB 凭据（TD-018 非-owner 服务角色切换）
#
# 用途：compose env_file 中排在 platform.env 之后 → 仅对 5 个平台服务容器
#   （auth-bff/website-bff/console-bff/admin-bff/model-platform）覆盖
#   DATABASE_URL 为非-owner `platform_svc`，并补 `reporting_ro` 只读池。
# 职责分离：owner（vxture）连接保留在 platform.env——DDL/seed/verify
#   （28/29/30 脚本、db-init workflow）继续以 owner 执行，不受本文件影响。
#   列级锁（98_column_locks.sql）只对非-owner 生效，owner 通道即迁移通道。
# 生成：真实文件由 32-provision-service-db-roles.sh 在 worker-01 生成
#   （随机密码 + ALTER ROLE + 写 /srv/vxture/runtime/secrets/platform-app.env，0600）。
#   禁止把真实密码提交进仓库。
# ═══════════════════════════════════════════════════════════════════════════
DATABASE_URL=postgresql://platform_svc:REPLACE_ME_platform_svc@vx-platform-pg:5432/platform_main
REPORTING_RO_DATABASE_URL=postgresql://reporting_ro:REPLACE_ME_reporting_ro@vx-platform-pg:5432/platform_main

# Dependencies
node_modules/
**/node_modules/
.pnp
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# Package manager lock files (keep pnpm-lock.yaml)
package-lock.json
yarn.lock

# Testing
coverage/
.nyc_output/
*.lcov

# Prisma generated clients (regenerated by prisma generate in CI)
**/src/generated/

# Build outputs
.next/
**/.next/
out/
**/out/
build/
**/build/
dist/
**/dist/

# Environment variables
.env
.env*.local
.env.production
.env.development
.env.test
# Deploy bundle must keep templates only; real runtime secrets belong to runtime/ or /srv/vxture/runtime.
deploy/**/.env
deploy/**/.env.*
!deploy/**/.env.example
!deploy/**/.env.*.example
deploy/**/secrets/pg-password
deploy/**/secrets/redis-password
deploy/**/secrets/platform.env
deploy/**/secrets/platform-mail.env
!deploy/**/secrets/*.example
!deploy/**/secrets/.gitkeep

# Local runtime env/secrets. These files are real secrets and must not be committed.
runtime/.env
runtime/.env.*
runtime/secrets/
runtime/local/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE and editor files
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/settings.json.template
!.vscode/*.template

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# TypeScript cache
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

# Storybook build outputs
storybook-static/

# Temporary folders
tmp/
temp/

# Python (for backend)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Jupyter Notebook
.ipynb_checkpoints

# Database
*.db
*.sqlite
*.sqlite3

# Cache directories
.cache/
.parcel-cache/

# Turborepo
.turbo/
# Editor directories and files (handled above)
# Local development certificates
*.pem
*.key
*.crt

# -----------------------------
# Additional project-specific ignores (added by analysis)
# These are local/runtime or editor files that should not be committed.
# Remove from tracked files with: git rm --cached <path>

# PID / runtime files
*.pid
.vxture_frontend.pid

# Local virtual environments (ensure any venvs under packages are ignored)
**/.venv/
**/venv/

# Editor temp / backups
.vscode/*.bak
.vscode/*.local
.vscode/settings.json.bak
.claude/settings.local.json
.claude/*.bak-*

# Log files
*.log

# Optional cache / local-only files
.cache/
.work-in-progress/

# Generated architecture diagrams (local tooling output, not versioned)
docs/architecture/package-architecture.html

# Local-only reference / personal notes (not versioned)
# - vpn-proxy note is a per-workstation baseline (self-declared local-only)
# - design-system-gallery is a regenerable claude.ai design import: large binaries
#   (multi-MB JPEGs) and raw-color mocks that trip the DS guardrail
/vpn-proxy-claude-cli-baseline.md
packages/design/design-system-gallery/

# End of additional ignores

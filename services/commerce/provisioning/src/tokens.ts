/** DI tokens for the provisioning dispatcher (P4). */
export const PROVISIONING_PG_POOL = "PROVISIONING_PG_POOL";

/** Resolves a `webhook_secret_ref` to the actual HMAC secret (secret manager). */
export const WEBHOOK_SECRET_RESOLVER = "WEBHOOK_SECRET_RESOLVER";

/** Sink for delivery exhaustion alerts (ops). */
export const PROVISIONING_ALERT_SINK = "PROVISIONING_ALERT_SINK";
